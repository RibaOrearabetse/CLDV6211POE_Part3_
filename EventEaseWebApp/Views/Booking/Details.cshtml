@model EventEaseWebApp.Models.Booking

@{
    ViewData["Title"] = "Details";
}

<h1>Booking Details</h1>

<div>
    Event_Ease.Models.Booking

    @{
        ViewData["Title"] = "Details";
    }

    <h1>Booking Details</h1>
    <hr />

    <div class="form-group">
        <label>Event</label>
        <input class="form-control" value="@Model.Event?.EventName" readonly />
    </div>
    <div class="form-group">
        <label>Venue</label>
        <input class="form-control" value="@Model.Venue?.VenueName" readonly />
    </div>
    <div class="form-group">
        <label>Booking Date</label>
        <input class="form-control" value="@Model.BookingDate.ToString("yyyy-MM-dd")" readonly />
    </div>

    <div>
        <a asp-action="Index">Back to List</a>
    </div>
